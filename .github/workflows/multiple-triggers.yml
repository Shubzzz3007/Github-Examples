name: Workflow that triggers on Multiple Events

on:
  push:
    branches:
      - main
      - dev
    
  pull_request:
    branches:
      - main
jobs: 
  Hello_World:
    runs-on: ubuntu-latest
    
    steps:
      - name: "Echo Basic information"
        run: |
          echo "REF: $GITHUB_REF"
          echo "Job ID: $GITHUB_JOB"
          echo "Action: $GITHUB_ACTION"
          echo "Actor: $GITHUB_ACTOR"



        